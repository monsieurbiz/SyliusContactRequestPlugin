{% set resource = hookable_metadata.context.resource %}
<div class="row">
    <div class="col-12 col-md-4 fw-bold">{{ 'monsieurbiz.contact_request.ui.email'|trans }}</div>
    <div class="col-12 col-md-8">
        {{ resource.email|default('') }}
    </div>
</div>
