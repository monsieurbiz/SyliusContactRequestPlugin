{% set resource = hookable_metadata.context.resource %}
<div class="row">
    <div class="col-12 col-md-4 fw-bold">{{ 'monsieurbiz_contact_request.form.name'|trans }}</div>
    <div class="col-12 col-md-8">
        {{ resource.name|default('') }}
    </div>
</div>
